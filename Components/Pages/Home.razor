@page "/"
@rendermode InteractiveServer

<PageTitle>Home</PageTitle>

<h1>Hello, world!</h1>

Welcome to your new app.

<button class="btn btn-primary" @onclick="() => ShowModal = true">Modal öffnen</button>

<Modal IsOpen="ShowModal" IsOpenChanged="@(v => ShowModal = v)">
    <h2>Mein Modal</h2>
    <p>Hier steht ein Text im Modal.</p>
</Modal>
@code {
    private bool ShowModal = false;
}